/*
 * New Archi Script
 */
 
console.clear();
load(__DIR__+'/lib/_util.ajs')
load(__DIR__+"/lib/_fmt_lib.ajs")
var element = $(selection).first()
// console.log(Math.pow(10, 3));
setZorderProperty(element)

		