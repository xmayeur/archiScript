load(__SCRIPTS_DIR__ + 'lib/lib.ajs')

$('concept').forEach(e => {
    try {
        embedProperties(e)
        e.prop().forEach(p => {
            if (p !== 'Identifier' || e.prop('Identifier') === '')
                e.removeProp(p)
        })
    } catch (e) {
    }
})