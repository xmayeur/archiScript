console.clear()
console.show()
load(__SCRIPTS_DIR__ + 'lib/lib.ajs')

$('element').forEach(e=>{
    if (!!e.prop('AT_DESC')) {
        e.prop('ARIS', e.id)
        e.prop('GUID', e.prop('UUID'))
        e.documentation = e.prop('AT_DESC')
        embedProperties(e)
    }
})

console.log('Done.')